[Unit]
Description=Check the tutoring-quiz main branch every minute

[Timer]
OnBootSec=2min
# Counted from the end of the previous check, so a long build never overlaps the next one.
OnUnitInactiveSec=1min
AccuracySec=10s

[Install]
WantedBy=timers.target
